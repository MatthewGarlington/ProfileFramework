import XCTest
@testable import ProfileFramework

final class ProfileFrameworkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ProfileFramework().text, "Hello, World!")
    }
}
